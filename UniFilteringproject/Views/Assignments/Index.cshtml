@model IEnumerable<UniFilteringproject.Models.Assignment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DaparNeeded)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProfileNeeded)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAboveMin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrMalAssinged)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinMalshabs)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DaparNeeded)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProfileNeeded)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAboveMin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrMalAssinged)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinMalshabs)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
